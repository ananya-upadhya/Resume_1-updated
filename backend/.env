# Builder Backend Environment Variables
# No API key needed for PDF/DOCX export functionality
# The builder backend runs on port 5000

# Optional: Set the analyzer backend URL for proxying enhance requests
ANALYZER_API_URL="http://localhost:8000"
